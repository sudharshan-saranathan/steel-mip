# RL: lower deployment ramp -- 10 Mt-steel/yr, H2 envelope 4 Mt (peak 1.0 Mt-H2/yr).
# Central EF/scrap.
let avg_emi        := 1.8;
let n8_scrap_rate  := 0.06;
let cap_add_common := 10000000;
let h2_ref_cap     := 4000000;
