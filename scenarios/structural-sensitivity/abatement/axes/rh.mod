# RH: higher deployment ramp -- 20 Mt-steel/yr, H2 envelope 8 Mt (peak 2.0 Mt-H2/yr).
# Central EF/scrap.
let avg_emi        := 1.8;
let n8_scrap_rate  := 0.06;
let cap_add_common := 20000000;
let h2_ref_cap     := 8000000;
