# EF1.6: emissions target tightened from 1.8 to 1.6 tCO2/tCS. Central scrap/ramp.
let avg_emi        := 1.6;
let n8_scrap_rate  := 0.06;
let cap_add_common := 15000000;
let h2_ref_cap     := 6000000;
