# Ramp level: LOW. 10 Mt-steel/yr common build-rate slab; H2 envelope scale
# 4 Mt -> peak electrolyser addition 0.25 x 4 Mt = 1.0 Mt-H2/yr.
let cap_add_common := 10000000;
let h2_ref_cap     := 4000000;
