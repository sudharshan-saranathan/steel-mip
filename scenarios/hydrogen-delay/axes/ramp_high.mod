# Ramp level: HIGH. 20 Mt-steel/yr common build-rate slab; H2 envelope scale
# 8 Mt -> peak electrolyser addition 0.25 x 8 Mt = 2.0 Mt-H2/yr.
let cap_add_common := 20000000;
let h2_ref_cap     := 8000000;
